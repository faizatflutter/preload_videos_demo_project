// Customize
const int kPreloadLimit = 3;

// Customize
const int kNextLimit = 10;

// For better UX, latency should be minimum.
// For demo: 1s is taken but something under a second will be better
const int kLatency = 1;
